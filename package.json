{
  "name": "virtuals-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ riccardoesclapon/virtuals-subgraph",
    "create-local": "graph create --node http://localhost:8020/ riccardoesclapon/virtuals-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ riccardoesclapon/virtuals-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 riccardoesclapon/virtuals-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.95.0",
    "@graphprotocol/graph-ts": "^0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
